# Optional ML/training dependencies for classifier training and export
transformers
datasets
scikit-learn
tokenizers
sentencepiece
huggingface-hub
pyarrow
s3fs
accelerate
mlflow
# Optional: peft, datasets[torch,s3], wandb
# To install: pip install -r requirements-ml.txt
# For datasets streaming from S3: pip install 'datasets[s3]'
